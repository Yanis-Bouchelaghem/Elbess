packages:
  - apps/*
  - packages/*

# pnpm 10 blocks dependency postinstall scripts by default. esbuild's script
# links its platform-native binary, which drizzle-kit needs to run.
onlyBuiltDependencies:
  - esbuild
